---
const { title, subtitle } = Astro.props;
---

<div class="flex mb-10">
  <div class="">
    <!-- class name in case anything goes wrong - education__time -->
    <span class="w-4 h-4 bg-neutral block rounded-full mt-1"></span>
    <span class="education__line bg-neutral block h-full w-[2px] translate-x-[7px]"></span>
  </div>
  <div class="experience__data bd-grid px-5">
    <h3 class="font-bold mb-1 text-xl">{title}</h3>
    <span class="text-lg font-semibold">{subtitle}</span>
    <p class="my-2 text-justify">
      <slot />
    </p>
  </div>
</div>
